import React from "react";

const Divider: React.FC = () => {
  return (
    <div
      style={{
        borderTop: "1px solid #ddd", // Horizontal line
        margin: "16px 0", // Space around the divider
      }}
    />
  );
};

export default Divider;
